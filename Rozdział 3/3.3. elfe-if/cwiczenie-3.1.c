/*
 Przedstawiona funkcja przeszukiwania binarnego wykonuje w pętli dwa testy.
 W rzeczywistości wystarczający jest jeden (kosztem dodatkowych warunków poza
 pętlą). Napisz wersję w jednym testem warunku wewnątrz pętli i zmierz
 różnicę czasów wykonania
*/

#include <stdio.h>

int binsearch(int x, int arr[], int n) {
  int low, mid, high;

  low = 0;

  while()
  return -1;
}
